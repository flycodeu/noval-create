---
名称: 向量召回与召回可观测
类型: 模块设计
所属模块: M04
优先级: P0
状态: 已完成
完成度: 100%
复核状态: 2026-04-16 已补真实召回快照持久化、章节级独立快照表、旧章节回填、质量聚合、连续降级阻断与章节批量自动暂停
上级索引: 13-百万字小说生成模块拆分与反AI味闭环设计.md
关联页面: 正文写作、质量监控、任务中心
关联服务: context.service、embedding.service、quality-dashboard.service、chapter.service、chapter-recall-runtime.service
关联数据表: chapter_embeddings、chapter_recall_runtime_snapshots、chapters、tasks
完成度判断: 每次生成都能追溯召回是否使用、命中多少、是否降级、是否命中过期片段；旧章节也能通过回填获得结构化快照而非退回启发式诊断。
---

# 向量召回与召回可观测

## 一、目标

让向量召回从“隐性辅助能力”变成“可追踪质量信号”。百万字生成中，召回服务异常或命中旧片段都会导致连续性退化，必须能被记录、展示和阻断批次。

## 二、当前已完成

- `chapter_embeddings` 已存章节片段，支持摘要、连续性、seed 等 fragment。
- `context.service.ts` 已使用多信号召回，查询角色、世界规则、支线、伏笔、连续性等历史信息。
- 召回失败时会降级到结构化状态，并写入 `recallDiagnostics`。
- 章节门已有“召回补充未依赖过期片段”检查。
- `chapter.service.ts` 在 Finalize 后会异步生成章节向量。

## 三、当前状态

- 每次章节流水线都会持久化结构化 `recallSnapshot / recallDiagnostics`，可按章节追溯真实运行结果。
- 召回失败已纳入章节门与章节批次总控，连续 3 章降级会阻断继续生成。
- 质量面板已统计连续降级章节数、bucket 覆盖率、平均命中数、过期召回率等指标。
- 老章节若缺少新的流水线任务快照，会回填当前状态的结构化 runtime snapshot，并显式标记 `backfilled` 来源。
- 仅在结构化快照彻底缺失时，才回退到启发式关键词诊断。

## 四、详细设计

### 4.1 召回快照

新增或复用任务输出记录以下字段：

```ts
retrieval_snapshots {
  id
  novelId
  chapterId
  taskId
  retrievalUsed
  hitCount
  bucketStatsJson
  staleRecallCount
  fallbackReason
  createdAt
}
```

`bucketStatsJson` 示例：

```json
{
  "character": { "hitCount": 3, "staleCount": 0 },
  "world": { "hitCount": 2, "staleCount": 1 },
  "thread": { "hitCount": 0, "staleCount": 0 },
  "foreshadow": { "hitCount": 1, "staleCount": 0 }
}
```

### 4.2 降级原因

标准化 `fallbackReason`：

- `embedding_service_failed`
- `query_embedding_failed`
- `no_hits`
- `only_stale_hits`
- `budget_trimmed`
- `disabled_by_config`

### 4.3 质量面板指标

新增召回健康指标：

- 召回可用率。
- 平均命中数。
- bucket 覆盖率。
- 过期片段率。
- 连续降级章节数。
- 最近一次降级原因。

### 4.4 批次暂停条件

建议规则：

- 连续 3 章召回失败：暂停批量生成。
- 单章 stale recall 超过 3 条：生成修复任务。
- 当前批次召回可用率低于 80%：批次结束后强制复盘。
- 召回失败但硬约束完整：允许单章继续，但标记风险。

## 五、实施步骤

1. 在 `context.service.ts` 的召回执行后形成 snapshot 对象。
2. 将 snapshot 写入新表或 `tasks.outputText/inputJson` 的结构化字段。
3. 在 `quality-dashboard.service.ts` 聚合召回健康趋势。
4. 在章节门中把连续降级作为 warning/blocker。
5. 在批次总控中接入暂停条件。

## 六、验收标准

- 每次生成都能看到 `retrievalUsed / hitCount / fallbackReason`。
- 召回失败不再是静默质量下滑。
- 质量面板能按卷/批次展示召回降级趋势。
- 用户能一键定位召回过期片段对应章节。
- 连续召回失败能阻止继续批量生成。

## 七、风险与建议

- 召回只能作为背景补充，不能覆盖硬约束和结构化状态。
- 不要把命中数高等同于质量高，必须同时监控 stale 率。
- 如果嵌入服务成本高，可先只持久化诊断，不立即扩大召回数量。

## 八、本次修复与复核

- `embedding.service.ts` 已补标准化召回降级原因，区分 `embedding_service_failed / query_embedding_failed / disabled_by_config / no_hits`。
- `context.service.ts` 已生成结构化 `recallSnapshot`，记录 `retrievalUsed / hitCount / fallbackReason / bucketStats`，并在软预算裁剪掉召回时标记 `budget_trimmed`。
- `chapter.service.ts` 已把 draft 阶段真实召回快照和诊断写入章节根任务 `progressJson`，用于事后追溯。
- `quality-dashboard.service.ts` 已优先消费真实任务快照；质量面板新增召回可用率、平均命中数、bucket 覆盖率、连续降级章节数、最近降级原因。
- `context-impact.service.ts` 已将“连续 3 章召回降级”提升为章节门 blocker，避免连续性风险继续放大。
- `batch-workflow.service.ts` 已新增独立 `chapter_batch_generate` 工作流，串行调度单章流水线，并在单章失败、章节门 `blocker`、连续 3 章召回降级时自动暂停，保留恢复游标与阻断章节信息。
- 正文写作页和质量面板已显示真实 `retrievalUsed / hitCount / fallbackReason`，不再只依赖启发式估算。
- 已通过 `context.service.test.ts`、`context-impact.service.test.ts`、`batch-workflow.service.test.ts` 与 `npm run typecheck` 验证。

## 九、当前剩余缺口

- 无。
