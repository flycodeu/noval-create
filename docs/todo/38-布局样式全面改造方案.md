# 38 - 布局样式全面改造方案

## 0. 当前状态（2026-04-26）

- 状态：进行中（按 2026-04-26 源码核对回写）
- 完成度：76%
- 更新依据：本轮按实际代码落地情况回写，未按预设 `100%` 强制覆盖

| 功能/范围 | 状态 | 完成度 | 当前代码结论 |
|----------|------|--------|--------------|
| ProjectSidebar | 已完成（当前轮） | 100% | 已迁移至 `ProjectSidebar.css`，移除核心内联样式并接入主题变量、hover/active 状态 |
| ProjectTopbar | 已完成（当前轮） | 100% | 已迁移至 `ProjectTopbar.css`，顶部栏样式与响应式布局已脱离内联 |
| NovelList | 部分完成 | 70% | 已新增 `src/pages/NovelList/index.css`，页头/统计/筛选/网格/向导骨架已迁出内联，仍保留少量动态展示样式 |
| Studio / Dashboard | 部分完成 | 78% | 已补 `dark` 变量与页面级覆盖，hero / 面板 / 卡片在深色主题下已可用，但比例与圆角规范仍未完全统一 |
| Writing | 部分完成 | 82% | `PipelineBar` 零内联，审校/回写/历史/并行分析等关键块已迁出主要布局内联，仅保留少量动态样式 |
| Characters | 部分完成 | 82% | 列表 / 图谱双工作区保持不变，并补齐图谱面板、节点、legend、切换 Tab 的 dark 适配 |
| 颜色变量统一 | 部分完成 | 80% | `global.css` 已落地 `--bg-app` / `--bg-surface` / `--text-primary` / `--accent` 语义 token，并向旧变量与 `--color-*` 提供别名映射 |
| 暗色主题适配 | 部分完成 | 82% | 已为 `Studio`、`Writing`、`Characters`、`Structure`、`Factions`、`MapExplorer` 补页面级 dark 覆盖，历史零散页面仍待继续扫尾 |
| 间距/字体/圆角规范化 | 部分完成 | 30% | 新增样式已按统一卡片和控件节奏收敛，但历史页面仍存在 16/18/20/22/24px 混用 |

## 一、当前样式问题总览

### 1.1 核心问题

| 问题类型 | 严重程度 | 影响范围 |
|----------|----------|----------|
| 内联样式泛滥 | 高 | ProjectSidebar、ProjectTopbar、NovelList 等全部组件 |
| 颜色体系混乱 | 高 | 20+ 不同色值散布在 CSS 和内联中 |
| 间距不统一 | 中 | 12/14/18/20/22px 无规律混用 |
| 暗色主题不完整 | 高 | 页面级 CSS 全部只有亮色适配 |
| 字体层级模糊 | 中 | 11-32px 无清晰层级体系 |
| 圆角不统一 | 低 | 6/8/12/16/18/20/22/24/32px 混用 |
| 侧边栏宽度不一致 | 中 | 236/280/300/320px 不同页面不同值 |

### 1.2 内联样式问题

**ProjectSidebar.tsx**：已在当前轮迁移至 `ProjectSidebar.css`，核心结构不再依赖内联样式。

**ProjectTopbar.tsx**：已在当前轮迁移至 `ProjectTopbar.css`。

**NovelList/index.tsx**：页头、统计卡片、筛选栏、卡片网格和主要向导结构已迁出到 `src/pages/NovelList/index.css`，仍有少量动态展示样式保留。

---

## 二、颜色体系改造

### 2.1 当前问题

CSS 变量体系存在**两套并行**的命名：

```css
/* 旧体系 */
--bg-page, --bg-panel, --bg-soft
--text-main, --text-sub, --text-muted
--border-light, --border-strong
--primary, --primary-hover, --primary-soft

/* 新体系 */
--color-bg-primary, --color-bg-secondary, --color-bg-card
--color-text-primary, --color-text-secondary, --color-text-muted
--color-blue-primary, --color-blue-dark
```

两套变量值不一致，页面级 CSS 使用旧体系，组件内联使用新旧混用。

### 2.2 改造方案

**统一为一套语义化变量**：

```css
:root {
  /* === 背景层级 === */
  --bg-app:      #F5F6F8;    /* 最底层页面背景 */
  --bg-surface:  #FFFFFF;    /* 卡片/面板表面 */
  --bg-elevated: #FFFFFF;    /* 弹窗/浮层 */
  --bg-muted:    #FAF7F1;    /* 柔和区块背景 */
  --bg-hover:    #EEF2F6;    /* 悬停态 */
  --bg-active:   #F3E7D8;    /* 选中/激活态 */

  /* === 文字层级 === */
  --text-primary:   #1F2937; /* 标题、正文主色 */
  --text-secondary: #6B7280; /* 副标题、说明文字 */
  --text-muted:     #9CA3AF; /* 辅助文字、占位符 */

  /* === 边框 === */
  --border-default: #E5E7EB;
  --border-strong:  #D1D5DB;

  /* === 主题色 === */
  --accent:       #A66A2B;   /* 主操作色 */
  --accent-hover: #8F5720;
  --accent-soft:  #F3E7D8;   /* 低饱和度背景 */

  /* === 语义色 === */
  --success: #2F855A;
  --warning: #B7791F;
  --danger:  #C2410C;
  --info:    #2563EB;

  /* === 圆角（只保留 4 级）=== */
  --radius-s:  6px;
  --radius-m:  10px;
  --radius-l:  14px;
  --radius-xl: 20px;

  /* === 间距（只用 4px 的倍数）=== */
  --space-xs:  4px;
  --space-s:   8px;
  --space-m:   12px;
  --space-l:   16px;
  --space-xl:  24px;
  --space-2xl: 32px;

  /* === 阴影 === */
  --shadow-sm:  0 1px 3px rgba(0,0,0,0.06);
  --shadow-md:  0 4px 12px rgba(0,0,0,0.08);
  --shadow-lg:  0 12px 24px rgba(0,0,0,0.1);
  --shadow-xl:  0 20px 40px rgba(0,0,0,0.12);
}
```

### 2.3 当前状态（2026-04-26）

**状态：部分完成（80%）**

- `src/styles/global.css` 已补齐 `--bg-app`、`--bg-surface`、`--bg-elevated`、`--text-primary`、`--border-default`、`--accent` 等语义 token。
- 旧体系 `--bg-page` / `--bg-panel` / `--primary` 与现有 `--color-*` 已改为映射到同一语义层，双轨漂移明显收敛。
- `light / dark / soft` 三套主题都已同步到新语义变量，并补了 workspace 变量在深色 / 柔和主题下的映射。
- 剩余问题主要是历史页面尚未把所有局部硬编码色值直接替换为新 token。

---

## 三、各页面布局改造详案

### 3.1 NovelList（小说列表页）

**当前状态（2026-04-26）：部分完成（70%）**

- 列表页已具备标题、统计卡片、筛选栏、卡片网格和创建向导。
- 已新增 `src/pages/NovelList/index.css`，页头、统计区、筛选栏、卡片网格、极速开书向导和标题建议侧栏都已迁出核心内联样式。
- 当前网格已收敛到 `repeat(auto-fill, minmax(320px, 380px))`，避免宽屏卡片失控拉伸。
- 页面仍保留少量题材卡片等动态展示样式，尚未做到完全零内联。

**当前问题**：
- 页头统计卡片用内联 grid，间距不一致
- 搜索/筛选栏布局紧凑，标签和控件混杂
- 小说卡片网格 `minmax(360px, 1fr)` 在宽屏下卡片太宽
- 创建向导 Modal 步骤太密

**改造布局**：

```
┌──────────────────────────────────────────────────────────┐
│  页头区 (padding: 24px 32px)                              │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  标题行: "我的作品" + [新建小说] 按钮                   │ │
│  │  (flex, justify-between, align-center)               │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                          │
│  统计卡片区 (grid, 4 列, gap: 16px, max-width: 840px)     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│  │ 作品总数  │ │ 进行中   │ │ 总字数   │ │ 本周更新 │        │
│  │ 数值     │ │ 数值    │ │ 数值     │ │ 数值     │        │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │
│                                                          │
│  筛选栏 (flex-wrap, gap: 12px, margin-top: 20px)          │
│  [🔍 搜索框 240px] [状态: 全部▾] [排序: 最近更新▾]         │
│                                                          │
│  ─────────────────────────────────────────────────────── │
│                                                          │
│  小说卡片网格 (grid, auto-fill, minmax(320px, 380px))      │
│  gap: 20px, justify-content: start                       │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐      │
│  │ 题材渐变色头   │ │              │ │              │      │
│  │ 标题          │ │              │ │              │      │
│  │ 简介(3行截断)  │ │              │ │              │      │
│  │ ─────────── │ │              │ │              │      │
│  │ 进度 + 字数   │ │              │ │              │      │
│  │ 最后更新时间   │ │              │ │              │      │
│  │ [进入] [更多▾] │ │              │ │              │      │
│  └──────────────┘ └──────────────┘ └──────────────┘      │
│                                                          │
│  空状态 (居中, 插图 + 引导文案 + 按钮)                      │
└──────────────────────────────────────────────────────────┘
```

**关键 CSS 规范**：
- 卡片固定 `min-width: 320px; max-width: 380px`，避免宽屏拉伸
- 卡片网格使用 `justify-content: start` 而非默认 stretch
- 统计卡片 `max-width: 840px`，不需要占满全宽
- 筛选控件使用 `flex-wrap: wrap` 确保窄屏不溢出

### 3.2 ProjectSidebar（项目侧边栏）

**当前状态（2026-04-26）：已完成（当前轮，100%）**

- `src/components/novel/layout/ProjectSidebar.tsx` 已迁移到 `ProjectSidebar.css`。
- 信息头、工作模式、导航组、active 态、hover 态均已收口为可复用 CSS 类。
- 已接入主题变量并补充 dark / soft 适配，不再依赖核心白底内联实现。

**当前问题**：
- 全部内联样式，暗色主题完全失效
- 背景硬编码 `#fff`
- 间距 6/8/10/12/16px 混用
- 导航项没有 hover 过渡效果

**改造布局**：

```
┌─── ProjectSidebar (width: 280px, 固定) ──────┐
│                                               │
│  信息头 (padding: 16px)                        │
│  ┌───────────────────────────────────────────┐ │
│  │  小说标题 (18px, 700, 最多2行)              │ │
│  │  当前阶段: 世界底盘  (12px, --text-muted)   │ │
│  │  模块完成: 5/13     (12px, --text-muted)   │ │
│  │  ┌─────────────────────────────────────┐  │ │
│  │  │  当前主任务 (bg: --bg-muted)         │  │ │
│  │  │  "完善角色关系网络"                    │  │ │
│  │  └─────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────┘ │
│                                               │
│  模式切换 (padding: 0 16px)                    │
│  ┌───────────────────────────────────────────┐ │
│  │  [● 快速模式] [○ 专业模式]                  │ │
│  │  (两个按钮, 选中态高亮, gap: 8px)            │ │
│  └───────────────────────────────────────────┘ │
│                                               │
│  导航组 (scrollable, flex: 1)                  │
│  ┌───────────────────────────────────────────┐ │
│  │  ▼ 基础设定 (3/4)                          │ │
│  │     概览          ● 已完成                  │ │
│  │     项目简报       ● 已完成                  │ │
│  │     故事核心       ● 已完成                  │ │
│  │     文风设定       ○ 未开始                  │ │
│  │  ▶ 世界资产 (0/5)                          │ │
│  │  ▶ 故事结构 (1/4)                          │ │
│  │  ▶ 章节生产 (0/3)                          │ │
│  │  ▶ 质量控制 (0/2)                          │ │
│  └───────────────────────────────────────────┘ │
│                                               │
└───────────────────────────────────────────────┘
```

**关键 CSS 规范**：
- 所有 `#fff` 替换为 `var(--bg-surface)`
- 所有 `rgba(166, 106, 43, ...)` 替换为 `var(--accent-soft)` / `var(--accent)`
- 导航项 hover：`background: var(--bg-hover); transition: var(--transition-fast)`
- 导航项 active：`background: var(--bg-active); border-left: 3px solid var(--accent)`
- 间距统一：组内 gap `8px`，组间 gap `12px`

### 3.3 Studio/Dashboard（项目控制台）

**当前状态（2026-04-26）：部分完成（70%）**

- `src/pages/Novel/Studio/index.tsx` 与 `index.css` 已形成独立 dashboard 结构，包含 hero、指标区、阶段区、行动卡片和健康面板。
- Hero、指标条和主内容区已经明显重构，说明该页是 38 文档里推进最快的部分。
- 但当前 CSS 仍大量使用 16/18/20/22/24px 圆角与间距混值，也未完全体现文档中强调的 `60/40`、`3:2` 和统一卡片高度目标。
- 页面样式仍主要依赖页面内自定义变量，尚未切换到文档第二节定义的统一语义 token。

**当前问题**：
- 信息密度高但层级不清晰
- "当前任务"和"就绪度"并排放在 hero 区，视觉权重相同
- 指标卡片网格和阶段面板之间缺乏视觉分隔
- 活动流和修订任务列表布局单调

**改造布局**：

```
┌──────────────────────────────────────────────────────────┐
│  Hero 区 (bg: 渐变, padding: 32px, border-radius: 20px)   │
│                                                          │
│  ┌─────────────────────────┐ ┌────────────────────────┐  │
│  │  当前最该处理             │ │  就绪度仪表盘           │  │
│  │  "完善角色关系网络"       │ │  ┌────────────────┐    │  │
│  │                         │ │  │  环形进度 72%    │    │  │
│  │  原因: ...              │ │  │  (Progress 环)   │    │  │
│  │  影响: 角色系统           │ │  └────────────────┘    │  │
│  │  阶段: 世界资产           │ │  底盘 ██████░░ 完成    │  │
│  │                         │ │  资产 ████░░░░ 进行中   │  │
│  │  [▶ 去处理] [查看影响]    │ │  结构 ░░░░░░░░ 未开始   │  │
│  │         60%             │ │         40%            │  │
│  └─────────────────────────┘ └────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  指标条 (grid, 5 列, gap: 16px)                           │
│  当前阶段 | 模块完成度 | 高优先风险 | 上下文版本 | 待回写     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  阶段面板 (grid, auto-fit, minmax(200px, 1fr), gap: 16px) │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 基础设定  │ │ 世界资产  │ │ 故事结构  │ │ 章节生产  │    │
│  │ 3/4 ✓   │ │ 0/5     │ │ 1/4     │ │ 0/3     │    │
│  │ 已完成   │ │ 进行中   │ │ 进行中   │ │ 未开始   │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
└──────────────────────────────────────────────────────────┘

┌──────────── 主内容区 (grid, 3:2 比例) ──────────────────┐
│  ┌────────────────────┐  ┌──────────────────────────┐   │
│  │  左栏 (60%)         │  │  右栏 (40%)               │   │
│  │                    │  │                          │   │
│  │  Blocker 列表       │  │  推荐下一步               │   │
│  │  (卡片堆叠)         │  │  (高亮卡片 + 行动按钮)     │   │
│  │                    │  │                          │   │
│  │  ─────────────────│  │  风险提示                  │   │
│  │                    │  │  (列表, 图标 + 文字)       │   │
│  │  最近活动流          │  │                          │   │
│  │  (时间轴式列表)      │  │  修订反推任务              │   │
│  │                    │  │  (卡片堆叠)               │   │
│  │                    │  │                          │   │
│  │                    │  │  常用入口                  │   │
│  │                    │  │  (网格按钮)               │   │
│  └────────────────────┘  └──────────────────────────┘   │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  健康面板 (grid, 3 列, gap: 16px)                         │
│  写作准备 | 连续性 | 结构体检                               │
└──────────────────────────────────────────────────────────┘
```

**关键改善**：
- Hero 区给"当前任务"60%宽度（核心信息），就绪度仪表盘40%
- 指标条使用统一高度卡片，不要大小不一
- 主内容区左宽右窄（3:2），"blocker + 活动"在左是操作流，"推荐 + 风险"在右是参考信息
- 阶段面板卡片等宽，hover 时边框高亮当前阶段

### 3.4 Writing（正文写作页）

**当前状态（2026-04-26）：部分完成（82%）**

- `src/components/novel/writing/PipelineBar.tsx` 已迁移到 `PipelineBar.css`，并上移为顶部 sticky 区。
- 页面已经开始把编辑、上下文、审校、历史等能力纳入同一工作台，而不是完全分散。
- 右侧焦点侧栏已进一步收口，合同 / 上下文 / 版本 / 审校入口被压缩到统一侧栏路径。
- 本轮已把审校确认弹层、历史版本区、伏笔回写、信息揭示控制、Explainability / Recall / Context Impact / Story Dynamics 等关键块的静态布局内联迁入 CSS。
- 当前仅保留少量依赖运行时颜色或尺寸的动态展示样式，尚未做到绝对零内联。

**当前问题**：
- 4097 行单文件，复杂度极高
- 多个子路由（Editor, Context, History, Review）各自独立布局
- PipelineBar 状态和编辑器主体的视觉层级不清晰
- 侧边面板（合同/上下文/版本）与主编辑区的比例不当

**改造布局**：

```
┌────────────────────────────────────────────────────────────────┐
│  PipelineBar (固定顶部, 高度 52px)                               │
│  [● 场景规划] → [● 初稿] → [○ 审校] → [○ 重写] → [○ 定稿]       │
│  左侧: 当前阶段标签  右侧: 进度百分比 + 耗时                       │
└────────────────────────────────────────────────────────────────┘

┌───────────────────── 主工作区 ──────────────────────────────────┐
│  ┌─────────────────────────────────┐ ┌───────────────────────┐ │
│  │  编辑器区 (flex: 1, min-w: 560px) │ │ 侧面板 (width: 360px) │ │
│  │                                 │ │                       │ │
│  │  章节标题 + 元信息行               │ │  Tab: 合同|上下文|版本  │ │
│  │  (章节号, 字数, AI检测分)          │ │                       │ │
│  │                                 │ │  合同 Tab:             │ │
│  │  ┌─────────────────────────┐    │ │   场景列表             │ │
│  │  │                         │    │ │   约束检查状态          │ │
│  │  │    正文编辑区域           │    │ │                       │ │
│  │  │    (单行间距1.8,          │    │ │  上下文 Tab:           │ │
│  │  │     最大宽度 72ch,        │    │ │   已注入上下文列表      │ │
│  │  │     居中排版)             │    │ │   token 预算使用情况    │ │
│  │  │                         │    │ │                       │ │
│  │  │                         │    │ │  版本 Tab:             │ │
│  │  │                         │    │ │   版本时间轴            │ │
│  │  │                         │    │ │   diff 对比入口         │ │
│  │  │                         │    │ │                       │ │
│  │  └─────────────────────────┘    │ │                       │ │
│  │                                 │ │                       │ │
│  │  底部操作栏                       │ │                       │ │
│  │  [生成下一章] [重新生成] [锁定]    │ │                       │ │
│  └─────────────────────────────────┘ └───────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

**关键改善**：
- 编辑器正文使用 `max-width: 72ch; margin: 0 auto` 居中排版（阅读体验）
- 侧面板固定 360px 宽度，用 Tab 切换内容（减少视觉噪音）
- PipelineBar 固定在顶部，不随页面滚动
- 底部操作栏 sticky bottom，始终可见

### 3.5 Characters（角色页）

**当前状态（2026-04-26）：部分完成（82%）**

- `src/pages/Novel/Characters/CharacterWorkspace.tsx` 已拥有角色列表、关系图谱和人物档案三个核心区域。
- 关系图能力已经落地到 `CharacterGraphCanvas`，并具备独立工作台样式。
- 当前已改成“人物列表 / 关系网络”两种工作区切换，关系图获得全宽视图。
- 本轮已补齐图谱看板、节点、legend、视图切换 Tab、草稿条与图容器的 dark 适配。
- 页面内部仍有少量表单控件和局部信息块保留历史样式写法，角色详情折叠化也还没完全收口。

**当前布局**：三列 grid (`280px | flex-1 | 360-460px`)

**问题**：
- 左侧角色列表和右侧属性面板同时可见时，中间编辑区太窄
- 关系图在第三列 460px 内显示效果差
- 1380px 断点下压缩到两列时布局跳动大

**改造方案**：

```
┌──────────────────────────────────────────────────────────┐
│  Tab: [角色列表] [关系网络]                                │
│                                                          │
│  角色列表 Tab:                                            │
│  ┌────────────────┐ ┌──────────────────────────────────┐  │
│  │ 角色侧栏 (280px) │ │ 角色详情 (flex: 1)               │  │
│  │                │ │                                  │  │
│  │ 搜索 + 筛选     │ │  基本信息 / 心理维度 / 弧线       │  │
│  │ 角色卡片列表     │ │  (垂直滚动, 分 Section 折叠)     │  │
│  │                │ │                                  │  │
│  └────────────────┘ └──────────────────────────────────┘  │
│                                                          │
│  关系网络 Tab:                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  ReactFlow 全宽展示 (min-height: 600px)               │ │
│  │  右下角: 浮动 minimap + legend                        │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

**关键改善**：
- 关系图不再挤在第三列，用 Tab 切换获得全宽展示
- 角色详情页用折叠 Section 替代三列布局
- 1380px 以下只需处理两列 → 一列的断点

---

## 四、全局样式修复清单

### 4.1 暗色主题适配

**当前状态（2026-04-26）：部分完成（82%）**

- `src/styles/global.css` 已存在 `[data-theme="dark"]` 与 `[data-theme="soft"]` 全局变量覆盖。
- `ProjectSidebar.css`、`ProjectTopbar.css`、`PipelineBar.css`、`NovelList/index.css` 已开始补充 dark/soft 下的表层适配。
- 本轮已为 `Studio`、`Writing`、`Characters`、`Structure`、`Factions`、`MapExplorer` 页面级 CSS 补齐显式 dark 覆盖块，并补了 workspace 变量的深色映射。
- 仍待继续处理的是未列入本轮范围的历史页面与更细碎的局部状态色统一。

**需要修改的文件**：

| CSS 文件 | 需要适配的内容 |
|----------|---------------|
| `Studio/index.css` | `--studio-ink`、`--studio-panel` 需要暗色变体 |
| `Writing/index.css` | 段落背景、分数卡片颜色 |
| `Characters/character-workspace.css` | 节点样式、图背景 |
| `Structure/index.css` | 拖拽手柄、卡片背景 |
| `Factions/index.css` | 图节点、面板背景 |
| `MapExplorer/map-explorer.css` | inspector 面板 |
| 所有组件内联样式 | `#fff` → `var(--bg-surface)` |

**通用方案**：在每个页面 CSS 末尾添加 `[data-theme="dark"]` 覆盖块。

### 4.2 间距规范化

**当前状态（2026-04-26）：未完成（20%）**

- `Studio`、`Writing`、`Characters` 等样式中仍可见 `6 / 8 / 10 / 12 / 14 / 16 / 18 / 22 / 24px` 的混用。
- 虽然全局 token 中已有部分 `space` 变量，但页面与组件尚未稳定收敛到单一 4px 倍数体系。

统一使用 4px 倍数体系：

| 场景 | 当前值（混用） | 规范值 |
|------|--------------|--------|
| 组件内部间距 | 6/8/10px | 8px (`--space-s`) |
| 元素间距 | 12/14px | 12px (`--space-m`) |
| 模块间距 | 16/18px | 16px (`--space-l`) |
| 区域间距 | 20/22/24px | 24px (`--space-xl`) |
| 大段落间距 | 28/30/32px | 32px (`--space-2xl`) |

### 4.3 字体层级规范化

**当前状态（2026-04-26）：部分完成（30%）**

- 当前页面已大量使用 `11px`、`12px`、`18px` 等常见层级，但仍存在 `13px`、`20px`、`28px` 等分散值。
- 说明样式正在向统一层级靠拢，但尚未严格落实到文档给出的 H1 / H2 / H3 / Body / Caption / Overline 体系。

| 层级 | 用途 | 大小 | 字重 | 行高 |
|------|------|------|------|------|
| H1 | 页面标题 | 24px | 700 | 1.3 |
| H2 | 区块标题 | 18px | 700 | 1.4 |
| H3 | 卡片标题 | 15px | 600 | 1.4 |
| Body | 正文 | 14px | 400 | 1.6 |
| Caption | 辅助说明 | 12px | 400 | 1.5 |
| Overline | 分类标签 | 11px | 700 | 1.2 |

### 4.4 圆角规范化

**当前状态（2026-04-26）：未完成（20%）**

- `Studio`、`Writing`、`Characters` 当前仍大量使用 `16 / 18 / 20 / 22 / 24px` 圆角值。
- 文档目标中的 `S / M / L / XL` 四级圆角体系尚未统一落地。

| 层级 | 当前值（混用） | 规范值 | 用途 |
|------|--------------|--------|------|
| S | 6/8px | 6px | 按钮、输入框、小标签 |
| M | 10/12px | 10px | 卡片、面板 |
| L | 14/16/18px | 14px | 大面板、模态框 |
| XL | 20/22/24px | 20px | Hero 区、全幅卡片 |

### 4.5 内联样式迁移优先级

**当前状态（2026-04-26）：部分完成（92%）**

- P0 项 `ProjectSidebar.tsx`、`ProjectTopbar.tsx` 已完成迁移。
- P1 项 `NovelList/index.tsx` 已完成主体迁移，并新增 `src/pages/NovelList/index.css`。
- `MetricCard.tsx`、`BlockerCard.tsx`、`ProjectCard.tsx` 已合并接入 `src/components/novel/cards/cards.css`。
- 当前剩余重点已转向 `NovelList` 少量动态展示样式和个别运行时色值，而不是核心列表/布局组件迁移。

| 优先级 | 组件 | 行数 | 迁移方式 |
|--------|------|------|----------|
| P0 | ProjectSidebar.tsx | 206 | 新建 `ProjectSidebar.css` |
| P0 | ProjectTopbar.tsx | 87 | 新建 `ProjectTopbar.css` |
| P1 | NovelList/index.tsx | ~600 | 新建 `NovelList.css` |
| P1 | MetricCard.tsx | ~60 | 新建 `cards.css` |
| P2 | BlockerCard.tsx | ~80 | 合并到 `cards.css` |
| P2 | ProjectCard.tsx | ~120 | 合并到 `cards.css` |

---

## 五、当前实施状态（基于 2026-04-26 源码核对）

### Phase 1：基础设施

- 状态：部分完成（80%）
- 已落地：`global.css` 已支持 `light / dark / soft` 主题变量，并补齐单一语义 token；旧命名与 `--color-*` 已改为映射到同一层。
- 未完成：历史页面仍未全面把局部硬编码值直接替换为语义 token。

### Phase 2：核心组件迁移

- 状态：部分完成（92%）
- 已落地：`ProjectSidebar`、`ProjectTopbar`、`NovelList` 主体已完成迁移；卡片组件已汇总到 `cards.css`；`PipelineBar` 也完成独立样式化；`Writing` 关键细节块已进一步迁出静态布局内联。
- 未完成：`NovelList` 与 `Writing` 仍保留少量运行时动态样式，尚未做到全域零内联。

### Phase 3：页面级布局优化

- 状态：部分完成（80%）
- 已落地：`Studio` 已完成较大幅度重构并补 dark 页面层；`Writing` 已继续压平关键功能块内联；`Characters` 已改成列表 / 图谱切换工作区并补 dark 图谱层。
- 未完成：`Studio` 与 `NovelList` 仍未完全收敛到统一 token 体系，`Writing` 仍保留少量动态样式。

### Phase 4：暗色主题适配

- 状态：部分完成（82%）
- 已落地：全局主题变量已提供 dark / soft 基础支持，新迁移组件和列表页样式已经开始补 dark / soft 表层适配；本轮已补齐文档点名页面的 dark 覆盖。
- 未完成：历史页面内部还存在未统一的色值与表层背景，soft 主题的页面级细化也还可以继续推进。
