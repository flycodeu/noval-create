---
名称: AI味深度分析与抑制方案
类型: 分析与修复
优先级: P0
状态: 部分完成
完成度: 72%
关联服务: anti-ai-rule.service、expression-dedup.service、chapter.service、quality-dashboard.service、generation-integrity.service、narrative-control.service
关联页面: 正文写作、质量监控
完成度判断: 长篇模式下不再固定盯最近10章；已具备近章/当前卷/全书采样三级表达去重与章首章尾/高潮结构轮换提示，并已接入写作链路和质量面板。
---

# 36 - AI 味深度分析与抑制方案

## 一、当前结论

当前系统的单章 anti-AI 抑制依旧很强，但真正会暴露“机器感”的问题主要出现在长篇累计阶段。  
本轮已把这里最危险的错误假设修掉：

- 不再把“最近 10 章”当作统一窗口。
- 不再默认短篇策略可以直接套到百万字长篇。
- 现已拆成 `短篇模式` 与 `长篇模式` 两套策略。

其中长篇模式的核心策略已经落地：

- `近章窗口`：直接禁复用，优先压当前批次的重复表达。
- `当前卷窗口`：做结构轮换提醒，防止卷内越写越像。
- `全书采样窗口`：记录长期同质化风险，避免百万字后半段持续复用同一写法。

## 二、六个维度的实际状态

### 2.1 跨章节表达复用

状态：已完成  
完成度：90%

本轮已完成：

- `expression-dedup.service.ts` 已从固定最近 `10` 章改成动态窗口。
- 已按小说规模自动切换 `short / longform` 模式。
- 长篇模式已输出 `近章 / 当前卷 / 全书采样` 三层窗口。
- 已新增跨章高频表达、章首、章尾、高潮结构复用识别。
- 结果已接入：
  - 写作生成链路
  - 章节存储 `expressionDedupJson`
  - 质量监控面板
  - 正文写作页风险摘要

仍存问题：

- 目前还是以短句和签名式结构为主，还不是完整语义级去重。
- 全书采样是稀疏采样，不是全量高成本比对。

### 2.2 情感高潮结构雷同

状态：部分完成  
完成度：70%

本轮已完成：

- 已新增 `repeatedClimaxPatterns` 检测。
- 已能识别高频高潮结构，如：
  - `长句蓄力后短句爆发`
  - `对话推进`
- 下一章 prompt 已会收到“高潮写法需要轮换”的硬提示。

仍存问题：

- 还没有把“高潮变体”做成显式 Scene Plan 字段。
- 还没有形成 6-8 种固定高潮模板的强制轮换库。

### 2.3 过渡段信息密度均匀化

状态：未完成  
完成度：20%

当前已有基础：

- `story-prompts.ts` 已有过渡节奏的文字引导。
- `narrative-control.service.ts` 已有叙事比例控制。

仍缺：

- 段落长度方差检测。
- “2-3 段密集 + 1 段极简”的过渡节奏约束。
- 审校后自动把“节奏过匀”写回下一章硬约束。

### 2.4 角色内心戏的平衡术

状态：部分完成  
完成度：40%

当前已有基础：

- 已有 POV 边界、感官覆盖、动作/对白/内心比例控制。
- 已能对“解释腔”“立场发虚”做跨章复现统计。

仍缺：

- 章节级“主情绪基调”字段。
- “同段对立情绪不得超过 2 个”的硬约束。
- 角色弧线中的“当前情绪阈值”挂钩。

### 2.5 世界观说明的教科书语气

状态：部分完成  
完成度：35%

当前已有基础：

- anti-AI 规则和 humanization 已能识别一部分说明文、解释腔、体裁空心化问题。
- 审校链已经能把部分世界观空心化问题回流。

仍缺：

- 专门的“信息传递方式”合同字段。
- 连续 3 句以上纯解释段的专项检测。
- 世界观章强制通过角色经历/互动展示的专门门禁。

### 2.6 章首章尾模式化

状态：部分完成  
完成度：75%

本轮已完成：

- 已新增 `repeatedOpenings / repeatedClosings` 识别。
- 已能检测最近章节与当前卷内的章首/章尾同质化。
- prompt 已会收到“本章优先改用其他开头/结尾方式”的轮换提示。
- 质量面板已展示章首、章尾、高潮结构同质化。

仍存问题：

- 还没有把 `openingStyle / endingStyle` 变成正式章节合同字段。
- 目前还是“强提示 + 轮换建议”，不是合同级强校验。

## 三、本轮实际改动

### 已完成

1. 长篇去重窗口重设计

- 短篇模式保留近章轻量窗口。
- 长篇模式启用 `近章 / 当前卷 / 全书采样` 三级窗口。
- 已彻底移除“统一最近 10 章”的核心实现假设。

2. 表达去重报告升级

- `ExpressionDedupReport` 已新增：
  - `mode`
  - `recentWindowSize`
  - `volumeWindowSize`
  - `globalSampleWindowSize`
  - `repeatedClimaxPatterns`
  - `volumeRepeatedPatterns`
  - `globalRepeatedPatterns`

3. 写作链路接入

- `chapter.service.ts` 已按当前章节 `volumeId` 生成长篇去重报告。
- Planner / Writer / Critic / Rewriter 四段 prompt 已复用新版去重提示。

4. UI 可视化接入

- `QualityDashboard` 已展示：
  - 当前模式
  - 近章窗口
  - 卷/全书窗口
  - 高频复用表达
  - 章首/章尾/高潮结构同质化
- `Writing` 页已展示当前章节所处的去重模式和窗口信息。

### 未完成

- 过渡段密度方差检测
- 情绪主基调硬约束
- 世界观说明文专项门禁
- 开头/结尾方式正式合同字段化

## 四、设计口径修正

以下旧思路不再作为实现依据：

- `前 10 章严格，11-50 章释放，50+ 章再收紧`

原因：

- 这套口径默认项目规模较小，更像中短篇或试跑策略。
- 对百万字长篇来说，风险单位不是“10 章”，而是：
  - 当前连续批次
  - 当前卷
  - 全书累计

现行口径改为：

- 短篇模式：小窗口、快反馈、轻量禁复用。
- 长篇模式：近章止血、卷内轮换、全书采样监控。

## 五、下一步优先级

### P0

- [x] 长篇窗口化表达去重
- [x] 章首章尾同质化识别
- [x] 高潮结构复用识别
- [x] 写作链路与质量面板接入

### P1

- [ ] 过渡段疏密检测
- [ ] 情绪主基调与单色情绪章节约束
- [ ] 世界观说明文专项检测

### P2

- [ ] 章节合同正式新增 `openingStyle / endingStyle / expositionMode / emotionFocus`
- [ ] 场景计划显式高潮变体字段
- [ ] 更深层的语义级相似结构去重
