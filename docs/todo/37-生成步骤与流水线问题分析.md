名称: 生成步骤与流水线问题分析
类型: 分析与修复
优先级: P0
状态: 已完成
完成度: 100%
关联服务: chapter.service、chapter-pipeline-policy.service、ai-engine.service、context.service、batch-workflow.service、prompt-override.service、prompt-library
关联页面: 正文写作、批量生成、章后回写
完成度判断: Chapter Bridge 已接入 Scene Plan / Draft / Review / Rewrite；Review 已新增服务端优先级摘要与 Rewrite 自适应策略；重写链路已支持相似度去重重试、轻量复检与人工介入分流；Provider 级 token 安全裕度已进入路由与上下文预算；写作页已展示回写等待状态、Prompt Override、章节指纹、一致性快检与下一章开场建议；批量生成已显式标注等待回写完成。
---

# 37 - 生成步骤与流水线问题分析

## 一、当前结论

原文里的大部分“问题”并不是完全未做，而是实现和文档状态脱节。  
本轮已经完成两件事：

- 把流水线里真正缺的能力补齐。
- 把已经存在但文档仍写成“待实现”的部分改成完成口径。

当前章节流水线仍是同一条主链：

`Scene Plan → Draft → Review → Rewrite → Canon / Finalize`

但关键差异已经落地：

- Scene Plan 已带 Chapter Bridge 承接计划，不再只看单章内分解。
- Review 已有优先级摘要，Rewrite 不再平均处理所有小问题。
- Rewrite 已支持自适应温度、上下文覆盖、相似度重试与轻量复检。
- Finalize 输出现在会明确带出一致性快检和下一章开场建议。
- 批量生成遇到回写未完成时，会明确显示“等待回写完成”。

## 二、各问题项状态

说明：以下各项按“已接入主链路、可见、可测试”作为完成标准；剩余内容仅保留为可选增强，不影响本项完成状态。

### 2.1 Scene Plan 章间衔接

状态：已完成（当前轮）  
完成度：100%

当前状态：

- `Chapter Bridge` 已在 `generation-integrity.service.ts` 落地。
- 已生成地点承接、时间跳跃、情绪承接、首场景约束、POV 边界和 warning。
- Planner / Draft / Review / Rewrite 四段 prompt 均已接入该桥接信息。

本轮修复：

- 文档口径改为“已完成”，不再误写为待实现。
- 写作页继续展示 `bridgePlan` 摘要，作为可视化核对入口。

可选增强：

- 如果后续需要更强约束，可继续补“桥接失败直接退回重排”的硬门。

### 2.2 Review 权重化

状态：已完成（当前轮）  
完成度：100%

本轮已完成：

- 新增 `chapter-pipeline-policy.service.ts`。
- 已把 `critical_fixes / continuity_risks / arc_progress_risks / reader_hook_risks / missing_payoffs` 归为高优先问题。
- 已生成 `reviewPrioritySummary`：
  - `topIssues`
  - `deferredIssues`
  - `rewriteScope`
  - `requiresFullRewrite`
- Rewrite prompt 已显式注入“重写优先级摘要”，不再把全部审校项平铺塞给 Rewriter。

当前效果：

- 大问题优先修。
- 语言级、对白级次要问题可延后。
- `critical_fixes >= 3`、合同 blocker、高风险结构问题会触发整章重写策略。

### 2.3 Rewrite 自适应策略

状态：已完成（当前轮）  
完成度：100%

本轮已完成：

- `ai-engine.service.ts` 的 `buildAiModelRouteReport()` 已支持阶段级覆盖。
- 重写阶段现已按审校结果自适应调整：
  - 常规重写维持原上限
  - 高风险重写温度上限提升到 `0.70`
  - 连续性 / 上下文 / 弧线类问题会切到 `max_coverage`
- `chapter.service.ts` 会在高覆盖策略下重建 rewrite context，而不是只改说明文字。
- finalize 前新增轻量复检；若整章重写后与初稿仍高度相似，会直接转 `human_review_required`。

本轮补齐的去重重试：

- Rewrite 首轮输出若与初稿过近，会自动进入一次 variation retry。
- retry prompt 会附带 `避免方向` 与 `创意方向提示`。

### 2.4 Finalize 收尾能力

状态：已完成（当前轮）  
完成度：100%

当前状态：

- Finalize 原本已负责摘要、连续性、长期记忆、实体发现、embedding、对白指纹刷新。

本轮修复：

- finalize 成功说明现在会显式带出：
  - 一致性快检摘要
  - 下一章开场建议
- 写作页已把以下内容集中展示：
  - `章节指纹`：复用对白指纹摘要
  - `一致性快检`：复用章节门 summary
  - `下一章开场建议`：复用 `nextChapterSeed`

说明：

- 这里优先复用现有存储结果，没有再引入新的 finalize 阶段或冗余 schema。

### 2.5 批量生成与回写时序

状态：已完成（当前轮）  
完成度：100%

当前状态：

- 批量生成本来就已有“每章成功后检查 writeback 是否 ready”的暂停逻辑。

本轮修复：

- `batch-workflow.service.ts` 的暂停文案已统一改成“等待章后回写完成”。
- 批量进度里继续保留 `currentWritebackStatus`，用于 UI 和任务状态展示。
- 写作页已新增：
  - 回写阶段
  - 下一章是否就绪
  - 回写失败原因 / 阻断说明

结果：

- 现在不仅顺序控制存在，而且等待原因对用户可见。

### 3.1 Prompt Override 风险

状态：已完成（当前轮）  
完成度：100%

当前状态：

- `prompt-override.service.ts` 已有保护 footer 与审计日志。
- 章节相关 key 已纳入保护范围：
  - `scenePlan`
  - `chapterDraft`
  - `chapterWriting`
  - `chapterReview`
  - `chapterRewrite`

本轮修复：

- `AiExplainabilityReport` 已新增 `activePromptOverrideKeys`。
- 写作页现在可以看到当前章节活跃的 Prompt Override。
- 流水线元数据也会直接显示当前使用中的 override key。

### 3.2 Variation 输出去重

状态：已完成（当前轮）  
完成度：100%

本轮已完成：

- `prompt-library.ts` 的 Scene Plan / Draft / Review / Rewrite prompt 已支持：
  - `attemptNumber`
  - `rejectedDigests`
  - `避免方向`
- Rewrite 首轮结果若与初稿过近，会自动触发一次变体重试。
- 修复链路里的二次 repair 也会携带 rejected digest，避免重复生成同一种失败方向。

结果：

- 重试不再只是固定提示词轮换，而是带着“避开前次失败方向”继续生成。

### 3.3 Token 安全裕度

状态：已完成（当前轮）  
完成度：100%

本轮已完成：

- `model.service.ts` 已新增 provider 级安全裕度：
  - `OpenAI`: 10%
  - `Anthropic`: 12%
  - `其他 / 国产 / custom`: 15%
- `ai-engine.service.ts` 路由预算已按 provider 裕度预留输出空间。
- `context.service.ts` 已引入：
  - `modelProvider`
  - `tokenSafetyMarginPct`
  - `safeModelContextLimit`

结果：

- 上下文预算和输出预算现在都按模型家族做安全收缩，不再默认统一 10%。

## 三、本轮实际改动

### 已完成

1. 流水线策略层

- 新增 `chapter-pipeline-policy.service.ts`
- 增加 Review 优先级汇总、Rewrite 自适应策略、轻量复检判断

2. 主链路接入

- `chapter.service.ts` 已接入：
  - Review 权重化
  - Rewrite route override
  - 相似度重试
  - Prompt Override explainability
  - finalize 输出增强

3. Prompt 层

- `prompt-library.ts` 已补 `避免方向` 和 chapter retry 参数支持

4. 路由与预算

- `ai-engine.service.ts` 已支持 provider 安全裕度和 rewrite route override
- `context.service.ts` 已支持安全上下文上限

5. UI 可视化

- `Writing` 页已展示：
  - 回写等待状态
  - Prompt Override
  - 章节指纹
  - 一致性快检
  - 下一章开场建议
  - finalize 角色状态

6. 批量生成

- `batch-workflow.service.ts` 已统一 writeback wait 文案

### 已补测试

- `chapter-pipeline-policy.service.test.ts`
- `ai-engine.service.test.ts`
- `story-prompts.test.ts`
- `context.service.test.ts`
- `batch-workflow.service.test.ts`

## 四、可选增强

以下内容保留为后续增强，不影响当前完成状态：

- 如果需要更硬门禁，可把 Chapter Bridge 失败直接升级为 replan 阻断。
- Rewrite 相似度重试目前默认一次，后续可扩成多 hint 轮换。
- Finalize 的“章节指纹”目前复用已有对白/门检/seed 结果，后续可再拆成独立持久化结构。
