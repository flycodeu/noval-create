---
名称: 长篇写作技术架构
类型: 深化
优先级: P2
状态: 部分完成
关联页面: 正文写作、创作总览、任务中心、质量监控
关联服务: context.service、chapter.service、task.service、planning-draft.service、story-memory.service
关联数据表: tasks、story_memory_checkpoints、chapters、chapter_segments
完成度判断: 具备角色分工流水线、合同驱动生成、Canonizer 闭环和可观察性后视为完成
---

# 长篇写作技术架构

## 一、目标

在保留现有上下文与章节流水线基础上，把系统升级成更稳定的多角色协作架构，用更少的漂移支撑更长的正文。

## 二、当前已完成

- [x] `context.service.ts` 已形成多层上下文：故事核心、当前弧线、人物状态、世界状态、线程压力、时间轴、长期记忆、硬约束、软上下文预算。
- [x] `chapter.service.ts` 已拆分写作阶段：`planning / drafting / reviewing / rewriting`。
- [x] `story-memory.service.ts` 已有长期记忆与 checkpoint 机制。
- [x] 任务系统已支持异步任务、工作流任务和规划草稿。

## 三、当前未完成

- [ ] 没有明确的 `Planner / Writer / Critic / Canonizer / Rewriter` 角色分工定义。
- [ ] 没有强制以 `章节合同 / 场景合同` 作为写作输入的架构约束。
- [ ] 没有独立的 Canonizer 可视化确认流程。
- [ ] 没有把写作失败、回写失败、门禁失败串成统一恢复链。
- [ ] 没有按角色化流水线展示每步质量与预算。

## 四、为什么现在要补

- 单一模型“一把梭”越长越不稳。
- 现有分层上下文已经够强，下一步关键是职责分离，而不是继续把所有摘要塞给同一个生成步骤。

## 五、建议新增内容

- 明确五个角色：
  - `Planner`：生成卷级、章节、场景合同
  - `Writer`：只按合同写正文
  - `Critic`：检查连续性、节奏、角色口吻、AI 味
  - `Canonizer`：从正文抽取事实并准备回写
  - `Rewriter`：只处理失败场景或失败段落
- 明确六层记忆使用原则：
  - `硬设定层`
  - `风格层`
  - `状态层`
  - `弧线层`
  - `章节合同层`
  - `场景局部层`

## 六、页面与交互

- 在任务中心中显示每个章节流水线由哪个角色阶段负责。
- 在正文写作页显示“合同生成、正文生成、审校、Canon 回写、重写”全链路状态。
- 在质量监控中展示不同角色阶段的失败分布与常见回退点。

## 七、数据结构 / 状态字段

- 现有 `tasks` 可继续复用，新增：
  - `pipelineRole`
  - `upstreamTaskId`
  - `contractVersion`
  - `canonDraftId`
  - `recoveryHint`
- 现有上下文结构继续保留，不推翻。

## 八、与现有模块的关系

- `context.service` 继续负责上下文分层与预算控制。
- `章节合同与场景合同` 成为 Planner 的主要产物。
- `章后状态回写中心` 成为 Canonizer 的交互面。
- `章节验收门` 成为 Critic 结果进入状态流转的出口。

## 九、验收标准

- 章节流水线的每一步都有清晰角色归属。
- Writer 不再直接消费泛化大纲，而是消费合同。
- Canonizer 结果可被人工确认并回写。
- 失败链路可明确区分“重写正文”“重跑合同”“重做回写”。

