# 模块缺口分析与扩展规划

> 初版日期：2026-04-06
> 回写日期：2026-04-07
> 状态：**核心项已实现，文档已按当前代码现状回写**

---

## 一、回写说明

本文件原本用于记录 GPT 提出的模块缺口及扩展方向。基于 2026-04-07 当前仓库代码复核后，原文中列为待补的核心模块大多已经落地，因此本文件不再维持“待开发规划”口径，而改为：

- 保留原分析框架
- 将各条目改判为 `已存在 / 已实现 / 待核验`
- 将真正尚未闭环的内容收敛为少量尾项

如果需要查看详细证据路径，参见：

- `docs/todo/模块缺口实现审计对照表.md`

---

## 二、逐项回写：当前状态

### 1. 受众与平台定位 — **已存在，无需新增**

`ProjectBrief` 仍完整覆盖以下信息：

| 字段能力 | 当前承载 |
|---|---|
| 目标读者 | `targetReader` + `targetAudience` |
| 发布平台 | `platformMode` |
| 核心卖点 | `sellingPoints` |
| 对标作品 | `compTitles` |
| 更新方式 | `deliveryRhythm` |
| 禁区 | `tabooRules` |
| 读者承诺 | `readerPromise` |

**回写结论：** 原判断成立，不需要拆独立模块。

---

### 2. 叙事策略 — **已实现**

原文认为 `ThemeVoice` 还应补 5 个字段：

- `protagonistCount`
- `viewpointMode`
- `parallelTimelines`
- `openingStyle`
- `flashbackPolicy`

当前这些字段已进入：

- 共享类型
- ThemeVoice 页面表单
- AI 生成与解析服务

**回写结论：** 此项已从“建议补充”升级为“已实现”。

---

### 3. 势力 / 组织 / 阵营系统 — **已实现**

原文建议新增 `factions` 表和对应 UI 模块。当前已确认存在：

- `factions` 数据表
- `faction.service.ts`
- Electron IPC 暴露
- 独立页面 `Factions`
- 主工作台路由入口
- 上下文摘要接入

已实现的核心能力包括：

- 势力基础信息维护
- 领袖绑定角色
- 地盘绑定地图节点
- 对外关系维护
- 工作流统计接入

**回写结论：** 此项已实现，不再属于缺口。

---

### 4. 角色关系网 — **已存在，无需新增**

当前仍保有：

- `characterRelations` 表
- 关系强度与动态历史等字段
- Characters 页面关系网络可视化
- ReactFlow 关系图

**回写结论：** 原判断成立，不需要新增模块。

---

### 5. 章节设计 / 章节卡 — **已存在，无需新增**

系统仍具备四层结构：

```text
StoryVolume -> StoryPart -> Chapter -> ChapterSegment
```

并由 Structure 页面承载层级规划与场景细化。

**回写结论：** 原判断成立，不需要新增“章节卡模块”。

---

### 6. 场景库 / 事件模板 — **已实现**

原文建议新增 `sceneTemplates`。当前已确认存在：

- `scene_templates` 表
- 内置 seed 数据
- 独立场景模板管理页
- 结构页套用模板能力

当前能力已覆盖：

- 内置模板
- 自定义模板
- 模板分类
- 节拍点与角色功能位
- 在场景规划时套用

**回写结论：** 此项已实现。

---

### 7. 伏笔与回收管理 — **已实现**

原文认为后端已有基础设施，但缺独立 UI。当前 `StoryThreads` 页面已具备：

- `线程看板 / 伏笔追踪` 双视图切换
- 待回收
- 即将到期
- 已回收
- 超期未收

并展示：

- 埋设章节
- 目标回收章节
- 当前距离
- 关联角色
- 回收条件

**回写结论：** 此项已实现，不再只是“后端有、前端缺”。

---

### 8. 设定词典 / 术语表 — **已实现，少量联动待核验**

原文建议新增 `glossary` 表。当前已确认存在：

- `glossary` 数据表
- 词典服务与 CRUD
- 独立页面
- 主路由入口
- 上下文摘要接入

已落地的核心价值：

- 固定术语、阶位、材料、事件等专有名词
- 别名管理
- 正式用法标记
- AI 上下文注入

仍未完全确认的尾项：

- `entity-discovery -> glossary` 的自动建议链路

**回写结论：** 核心模块已实现，但自动发现后的词典建议闭环仍待核验。

---

### 9. 书名 / 简介 / 卖点包装 — **已实现**

原文建议在 Overview 增加“包装助手”，并落到 `blurbJson`。当前已确认存在：

- `blurbJson` 字段
- 包装数据结构
- Overview 页面“包装助手”面板
- AI 生成包装文案能力

当前已支持：

- 书名候选
- 一句话钩子
- 起点版简介
- 番茄版简介
- 出版版简介
- 卷名风格

**回写结论：** 此项已实现，且实现范围已覆盖原文建议。

---

## 三、步骤重叠问题回写

原文认为以下四个模块职责并不重叠：

- 故事线程
- 故事设计
- 故事大纲
- 结构规划

当前代码工作台仍保持这一拆分，且路由、页面和工作流统计仍按此结构组织。

**回写结论：** 原判断成立，维持现状，不建议合并。

---

## 四、当前真实优先级

原文中的大部分 P1/P2/P3 项已完成，因此当前优先级需要重排。

### 已完成项

| 项目 | 当前状态 |
|---|---|
| 势力系统 | 已实现 |
| 设定词典 | 已实现 |
| 伏笔追踪视图 | 已实现 |
| 叙事策略扩展 | 已实现 |
| 场景模板库 | 已实现 |
| 包装助手 | 已实现 |

### 待核验 / 待收口项

| 项目 | 类型 | 当前判断 |
|---|---|---|
| `entity-discovery -> glossary` 自动建议 | 联动闭环 | 待核验 |
| 势力 / 术语上下文筛选细则 | 运行时策略 | 待核验 |

---

## 五、上下文管线影响回写

原文要求新增的两个核心模块接入 `context.service.ts`。当前状态如下：

### 势力系统接入

已确认：

- 势力摘要已进入上下文服务
- 关联角色的势力信息会被抽取进入上下文摘要

未逐条运行验收：

- 是否始终只注入“当前章节出场角色所属势力”

### 设定词典接入

已确认：

- 术语词典摘要已进入上下文服务
- 术语别名参与摘要生成

未逐条运行验收：

- 是否完全按“本章涉及术语”动态筛选
- 是否与 `entity-discovery` 形成自动建议闭环

**回写结论：** 上下文接入已完成，但筛选细则仍属于待核验项。

---

## 六、最终结论

这份文档现在应被理解为：

- 一份已经被验证过的扩展规划
- 其中大部分核心项已经完成
- 当前不应再将其视为“待开发清单”

推荐状态口径：

> **核心项已实现，剩余为少量联动收口与运行验收。**

---

## 七、后续动作

1. 保留本文件作为“已回写的规划记录”
2. 以后续尾项为中心单独立文，而不是继续维护整份缺口规划
3. 若继续推进，优先核对：
   - `entity-discovery -> glossary` 自动建议
   - 势力 / 术语上下文筛选是否符合运行时预期
